# Sesión 2026-07-12c
**Branch**: main
**Resumen**: Se implementó y validó la arquitectura modular v1 de PoliBot en n8n.

## Decisiones clave
- Separar Telegram, respuestas estáticas y Canvas en tres workflows para mantener responsabilidades claras.
- Usar `wf_telegram_router` como único workflow activo en producción; los sub-workflows se invocan internamente.
- Mantener el formato de tareas de `PoliBot_Inicial` y el timezone `America/Guayaquil`.
- Convertir saludos naturales como `hola` a la ruta `/start` sin añadir IA en v1.

## Archivos modificados
- Workflows n8n: `wf_telegram_router` (`vAaVfPUfv6tcyMrV`)
- Workflows n8n: `wf_cmd_static` (`iSTRgndQX0SeWvmS`)
- Workflows n8n: `wf_cmd_canvas_queries` (`ymE59cw5zVBWiktC`)
- `AGENTS.md`

## Pendiente para próxima sesión
- Publicar `wf_telegram_router` y probar `/start`, `/ayuda`, `/hoy`, `/semana` y `/proximas` desde Telegram.
- Mantener `PoliBot_Inicial` inactivo para evitar competir por el webhook del bot.
